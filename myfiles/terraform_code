terraform {
  required_providers {
    aws = {
        source = "hashicorp/aws"
        version = "~> 6.0"
    }
  }
}
provider "aws" {
    region = "ap-south-1"
  
}
resource "aws_s3_bucket" "my_bucket" {
    bucket = "mahtab-terraform-day01-20260922"

    tags = {
      Name = "bucket-5.0"
      Environment = "Prod"
    }
}
